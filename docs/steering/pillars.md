# Project Pillars: HyperAgent Zero

## Pillars

| Pillar | Current State | Done Criteria |
|--------|---------------|---------------|
| **MVP / Ship** | Not started. Upstream Agent Zero works in Docker mode. No v2 features implemented yet. | Host-first startup works, Claude SDK provider works, daemon start/stop/status works. Usable for single-project host-mode workflow. |
| **Test / Examples** | Upstream has minimal tests (`tests/`). No v2-specific tests. | Each spec has verification tasks. Host-mode startup tested without Docker. Channel + sandbox-registry tested. Provider round-trip tested. |
| **Design / Arch** | 5 specs drafted. CLAUDE.md seeded. Steering docs created. | Specs match code after implementation. No stale claims in specs or steering. Reference docs generated. |
| **Documentation** | Upstream docs in `docs/`. No v2-specific docs. | Updated README reflecting v2 capabilities. Installation guide for host-mode. Configuration guide for providers. |
